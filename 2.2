# 2.2: x_n = (3 - sqrt(7))^n = a_n + b_n*sqrt(7)
# Tính (a1000 + b1000) mod 999
# -----------------------------
mod = 999
a, b = 1, 0  # a0 = 1, b0 = 0
for n in range(1, 1001):
    a_new = (3*a - 7*b) % mod
    b_new = (3*b - a) % mod
    a, b = a_new, b_new

print("2.2) (a1000 + b1000) mod 999 =", (a + b) % mod)
